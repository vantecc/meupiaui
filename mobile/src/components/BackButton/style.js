import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'absolute',
    zIndex: 10,
    width: 50,
    height: 50,
    justifyContent: 'center',
    alignItems: 'center',
    top: 60,
    left: 20,
    borderWidth: 2,
    borderColor: '#ccc',
    backgroundColor: '#fff',
    borderRadius: '50%',
  },

  
  
});
