import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'absolute',
    zIndex: 10,
    padding: 8,
    marginTop: 40,
  },

  
  icon: {
    width: 35,
    height: 35,
    resizeMode: 'contain',
  },
});
